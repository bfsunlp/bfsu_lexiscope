# BFSU AlignLens unified full edition dependencies
# Python 3.10/3.11/3.12 recommended on Windows 11
#
# GPU/CPU-compatible PyTorch policy:
# - The CUDA-enabled PyTorch wheel below can run on NVIDIA GPU when CUDA is available.
# - It also falls back to CPU when no compatible GPU/driver is visible.
# - If your NVIDIA driver is older, change cu128 to cu126 or cu118 according to
#   the official PyTorch selector: https://pytorch.org/get-started/locally/
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.7.0+cu128
torchvision==0.22.0+cu128
torchaudio==2.7.0+cu128

numpy>=1.24
scipy>=1.10
openpyxl>=3.1
python-docx>=1.1
striprtf>=0.0.26
charset-normalizer>=3.3
sentence-transformers>=2.7
transformers>=4.40
tqdm>=4.66
openai>=1.40
lxml>=5.0
# Optional NLP segmenters; install when needed:
stanza>=1.9
spacy>=3.7
hanlp>=2.1
# sudachipy>=0.6
# fugashi>=1.3
# pywin32>=306      # optional Windows-only legacy .doc support if extended later
# pypandoc>=1.13    # optional alternative document conversion backend

Pillow>=10.0
